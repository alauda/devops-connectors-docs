# QA Packet — Gitlab automatic project and sub-account support using API and CLI

<!--
Generated by /feature:qa on 2026-05-13.
Sourced from tech-design.md `## Test Design`, feature.md `## Acceptance criteria`,
and state.yaml `bundle` block.
-->

## Bundle

- **Tag:** `v1.11.0-beta.141.g51835d3`
- **Image:** `build-harbor.alauda.cn/devops/connectors-operator-bundle:v1.11.0-beta.141.g51835d3@sha256:85019b602e9d3928854feaa5e4d66edc305c9733bf0c96a6e63c713181401687`
- **Synced manifest in this bundle:** `cmd/kodata/connectors-gitlab-tektoncd/1.0.0/install.yaml` (the rendered Task ships as part of the connectors-gitlab-tektoncd component).
- **Operator image:** `v1.11.0-g51835d3@sha256:9fdb5b35...`
- **Source PipelineRun:** `connectors-operator-rcfjd` (devops/business-build/devops, completed 2026-05-13T01:01:09Z). Two prior attempts failed on infra (Harbor maintenance + buildx DNS flake) — recorded at `/feature:integrate`, not QA.

## Jira

- **Story:** DEVOPS-43146 — Gitlab automatic project and sub-account support using API and CLI — [link](https://jira.alauda.cn/browse/DEVOPS-43146)
- **Parent epic:** DEVOPS-42609

## Acceptance criteria → test case map

Mapping is derived from `tech-design.md ## Test Design` (15 cases) and `feature.md ## Acceptance criteria` (11 ACs). BDD coverage lives in `connectors-extensions/connectors-gitlab/tektoncd/tasks/gitlab-connector-automatic-creation/0.1/testing/features/` — two files: `tektoncd.feature` (Task contract + e2e), `script.feature` (helper-unit Pod scenarios).

### AC-1 — tenant group + optional subgroups created/reused for both patterns

- BDD feature: `tektoncd.feature`
- Scenarios: 测试用例 1 (Pattern A fresh creation), 测试用例 2 (Pattern B fresh creation), 测试用例 5 (add-subgroup reconcile), 测试用例 8 (Pattern A admin lacks can_create_group), 测试用例 9 (Pattern B admin lacks owner)
- Expected outcome: tenant group + subgroups created idempotently for both pattern shapes; prerequisite mismatches fail cleanly with actionable messages.

### AC-2 — GAT provisioned at `tenantGroup` with requested `access_level` + `scopes`

- BDD feature: `tektoncd.feature`
- Scenarios: 测试用例 1, 2, 3 (rotate path), 4 (expired-token recreate), 6 (scopes change recreate), 7 (accessLevel change recreate)
- Expected: GAT exists at the tenant group with exact `scopes` and `access_level` requested.

### AC-3 — Token-refresh: rotate when identity-unchanged, recreate when identity-affecting input changed

- BDD features: `tektoncd.feature` (e2e), `script.feature` (helper-unit)
- Scenarios: 测试用例 3 (rotate), 测试用例 4 (expired → recreate), 测试用例 5/6/7 (recreate triggers)
- Expected: `POST /groups/:id/access_tokens/:token_id/rotate` for rotate path; delete-then-create for recreate path; expired GAT falls through to recreate.

### AC-4 — Cluster outputs: tenant `gitlab` Connector + auth Secret in connector namespace

- BDD feature: `tektoncd.feature` (post-run CEL assertions on each smoke scenario)
- Scenarios: 测试用例 1, 2, 4, 5, 6, 7
- Expected: Connector + `connectors.cpaas.io/gitlab-pat-auth` Secret server-side-applied with field-manager `connector-auto`.

### AC-5 — Admin creds via CSI mount only; raw PAT/GAT never in Pod spec or tenant Secret

- BDD feature: `script.feature`
- Scenarios: 测试用例 14 (`ensure-gat verbose=true 时不应 echo admin PAT`)
- Expected: helper scripts read admin creds from the mounted `gitlabconfig` files; logs never echo the raw token.

### AC-6 — Idempotency (rotate is non-destructive; identity change triggers recreate)

- BDD feature: `tektoncd.feature`
- Scenarios: 测试用例 3 (rotate-in-place), 4, 5, 6, 7 (controlled recreate)
- Expected: Connector `resourceVersion` does not bump on rotate; Secret data does.

### AC-7 — Actionable errors for the four prerequisite-mismatch cases + GitLab max-expiry rejection

- BDD features: `tektoncd.feature` (e2e), `script.feature` (helper-unit)
- Scenarios: 测试用例 8 (Pattern A lacks `can_create_group`), 测试用例 9 (Pattern B lacks `owner`), 测试用例 10 (path conflict), 测试用例 11 (max-expiry rejection — Task-level), 测试用例 11 unit (script.feature — `max_token_expiry 422` pass-through), 测试用例 12 (quota exhausted)
- Expected: Task fails at the first prerequisite gate; error message names the missing permission or constraint; no partial GitLab state; no cluster mutation.

### AC-8 — Task results populated

- BDD feature: `tektoncd.feature`
- Scenarios: 测试用例 1, 2 + the static result-declaration scenario `Task 应声明当前已实现的 4 个 results`
- Expected: `tenant-group`, `subgroups`, `access-token-name`, `connector-ref` all populated.

### AC-9 — Integration tests cover all six modes (mirror Harbor's feature-file layout)

- BDD feature: `tektoncd.feature` (full file)
- Scenarios: all of 测试用例 1–13 in `tektoncd.feature`
- Expected: BDD harness (`cd connectors-extensions/testing && make test`) runs all scenarios green; per-scenario tags align with Harbor's shape.

### AC-10 — Documentation: concept page + how-to with both pattern examples

- Source PR: connectors-operator#1002 (merged 2026-05-12, docs land in `connectors-operator/docs/en/connectors-gitlab/`)
- Method: manual review by reporter (Daniel Morinigo); how-to TaskRun examples walked through end-to-end against a fresh GitLab namespace.
- Expected: concept page covers `glab` CLI mount + the two patterns; how-to covers full Tekton Task with Pattern A + Pattern B end-to-end TaskRun examples, admin prerequisites, refresh-cron pattern, manual cross-group-permissions workaround.

### AC-11 — Non-root UID 65532 on linux/amd64 + arm64; CPU/mem requests+limits per step; emptyDir tmpfs for scripts + token

- BDD feature: `tektoncd.feature`
- Scenarios: `Task steps 应声明 non-root 安全上下文`
- Expected: CEL on `obj.spec.stepTemplate.securityContext` confirms `runAsNonRoot: true, runAsUser: 65532, runAsGroup: 65532, capabilities.drop: [ALL], allowPrivilegeEscalation: false`; CEL on `obj.spec.steps[].resources` confirms per-step requests/limits; CEL on `obj.spec.volumes` confirms `scripts` + `token` emptyDirs with `medium: Memory`.

## Environment

- **Cluster requirements:** ACP 4.3 (installer-v4.3.0-online), 1 global + 1 business single-node, kube-ovn IPv4, registry external `registry.alauda.cn:60070`. Provisioned via `idp-deploy-env` skill with `DevOpsStdAcpV2` claim (see [`reference_devopsstdacpv2_qa_env`](../../../../../.claude/projects/.../memory/reference_devopsstdacpv2_qa_env.md)).
- **Active env:** `daniel-5shk6` (display: `devops-43146-qa`).
- **Operator install:** Artifact + ArtifactVersion CRs in `cpaas-system` → Subscription with manual InstallPlan approve (see [`reference_artifactversion_install`](../../../../../.claude/projects/.../memory/reference_artifactversion_install.md)).
  - `connectors-operator.v1.11.0-beta.141.g51835d3` in ns `connectors-operator`, channel `alpha`.
  - `tektoncd-operator.v4.10.1` in ns `tekton-operator`, channel `stable` (BUSINESS cluster).
- **Connector CRs (BUSINESS cluster):** `ConnectorsCore`, `ConnectorsGitLab`, `ConnectorsK8S` in `connectors-system` ns (PSS=privileged).
- **Kubeconfig retrieval:**
  ```bash
  kubectl --context=alauda-idp -n idp get secret daniel-5shk6-j8f5s-service-access \
    -o jsonpath='{.data.kubeconfig}' | base64 -d > /tmp/daniel-5shk6-kubeconfig
  # Business cluster: same bearer token, swap /global → /business in the URL
  sed 's|/kubernetes/global|/kubernetes/business|g; s|name: global|name: business|g; s|cluster: global|cluster: business|g; s|current-context: global|current-context: business|g' \
    /tmp/daniel-5shk6-kubeconfig > /tmp/daniel-5shk6-business-kubeconfig
  ```
  Always `--insecure-skip-tls-verify=true`.
- **Feature flags / settings (TektonConfig — business cluster):**
  - `spec.pipeline.options.configMaps.feature-flags.data.set-security-context: "true"` — required for PSS=restricted ns (see [`feedback_pss_restricted_tekton`](../../../../../.claude/projects/.../memory/feedback_pss_restricted_tekton.md)).
- **Prerequisite test fixtures (manual setup for smoke):**
  - Project `test` with namespaces `test` (PSS=restricted; hosts shared Connectors) and `test-dev` (PSS=restricted; hosts TaskRuns).
  - Admin GitLab Connector `test/devops-gitlab-admin` pointing at `https://devops-gitlab.alaudatech.net`, auth Secret `devops-gitlab-admin-pat` carrying a `can_create_group` user PAT.
  - k8s Connector `test/incluster-k8s` pointing at `https://kubernetes.default.svc:443`, bearer-token Secret backed by SA `test/connector-writer` with Role granting Secret + connectors.alauda.io/connectors CRUD on ns `test`.
  - ConnectorClass `gitlab` patched with `connector_address: {{ .connector.spec.address | quote }}` in its `gitlabconfig` template (drift workaround — see Drift section below).

## Test instructions

- **BDD suite (Stories 1–2 — test cases 1–13 + script-only 14–15):** ran in connectors-extensions CI on PR #269 head commit; result green for `Pipelines as Code CI / gitlab-connector-automatic-creation`.
- **Operator pipeline-wiring (Story 4 — test case 14 indirect via sync-manifest pipeline):** the `sync-install-manifests` pipeline ran on the post-merge commit; bundle build pipeline `connectors-operator-rcfjd` succeeded.
- **Live smoke (cross-validates BDD against the deployed bundle):** two TaskRuns against `https://devops-gitlab.alaudatech.net`:
  - Pattern A: `tenantGroup=test-smoke`, `subgroups=[dev]` → Connector `test/test-smoke-gitlab` Ready+LivenessReady=True.
  - Pattern B: `tenantGroup=test-smoke/acme-b`, `subgroups=[dev]` → Connector `test/test-smoke-b-gitlab` Ready+LivenessReady=True.
- **How to interpret failure output:** TaskRun-level e2e logs surface step exit codes; helper-unit Pod scenarios surface `log_tail`. CEL assertion failures point at the failing field path. See `tech-design.md ## Test Design ### Testing format` for the assertion-table conventions.
- **Contact on blockers:** Daniel Morinigo (driver). Tag jtcheng on subgroup/GAT-semantic questions.

## Known drift (acknowledged in this packet — does not block advance)

1. **ConnectorClass `gitlab` `connector_address` line.** The connectors-extensions main HEAD (`d3d6148`) carries the new `connector_address: {{ .connector.spec.address | quote }}` line in `gitlabconfig.config.yml`, but the operator kodata at SHA `51835d3` predates this change (auto-sync PR did not fire post-PR-#269 due to PaC webhook drift). Workaround in this env: live ConnectorClass patch + `/connectors-gitlab` commit-comment fired on extensions main to publish the manifest to Nexus, which will produce the auto-sync PR on connectors-operator main. Once that PR lands + a new bundle ships, the live patch can be removed. Tracked as a follow-up; not a defect against the Task itself.
2. **PR #1000 `connector-operator-test` pipeline failure at merge time.** Admin-merged by Daniel after `/retest`; failure was not in the gitlab-tektoncd path. Subsequent bundle build (`connectors-operator-rcfjd`) succeeded with the same operator code. Treated as a non-blocking flake; no QA case fails because of it.

## Rollback

- **Per-environment rollback (this Task — no shared infra change):**
  1. Delete the TaskRuns: `kubectl -n test-dev delete taskrun --all`.
  2. Delete tenant Connectors + Secrets the Task created: `kubectl -n test delete connector,secret -l connectors.cpaas.io/created-by=gitlab-connector-automatic-creation`.
  3. Optionally revoke the corresponding GATs in GitLab via `glab` or the UI.
- **Operator rollback:** downgrade ArtifactVersion to the previous `connectors-operator.v1.11.0-*` tag; Subscription's manual InstallPlan flow approves the new (older) CSV; previous-version CSV reclaims the operator pod. Bundle delta in this build = one new `cmd/kodata/connectors-gitlab-tektoncd/1.0.0/install.yaml`; reverting drops that manifest from the ApplySet which removes the Task from the cluster. No CRD or webhook surface added.
- **Feature is opt-in:** tenants must explicitly create a TaskRun referencing `gitlab-connector-automatic-creation`. No existing user is migrated automatically. Rollback for existing users = "do not run new TaskRuns"; no cleanup needed for already-running tenants.

## QA owner

- Assigned: Daniel Morinigo (`daniel`)
- Assigned at: 2026-05-13T04:15:00Z
